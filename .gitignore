## User settings
xcuserdata/

# Xcode build
build/

# Swift Package Manager
.swiftpm
.build/

## App packaging
*.ipa
*.dSYM.zip
*.dSYM

# fastlane
#
# It is recommended to not store the screenshots in the git repo.
# Instead, use fastlane to re-generate the screenshots whenever they are needed.
# For more information about the recommended setup visit:
# https://docs.fastlane.tools/best-practices/source-control/#source-control
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

# SF extension bundle
#
# Bundling of JS code is part of project build process,
# we don't need to commit build files.
iOS (Extension)/Resources/background.js
iOS (Extension)/Resources/content.js
iOS (Extension)/Resources/manifest.json

# pnpm is used when building the Safari extension
.pnpm-debug.log

# This will be modified every time you run the build script
Config/LocalIP.xcconfig

# Fill on demand with prod bearer token
Config/PreviewBearer.xcconfig
